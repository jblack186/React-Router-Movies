import React from 'react';

const MovieCard = props => {
  // let id = props.match.params.id;
  // let card = props.movies.find(card => card.id === Number(id))
console.log(props)
  return(
    <div>
      {/* <h2>{card.title}</h2> */}
    </div>
  );
};

export default MovieCard;
